[build]
  command = "cd .. && npm install && npm run docs:build"
  publish = "build"

# Root `prepare` installs husky; Netlify production installs omit
# root devDependencies, so husky is absent. Skip the hook install.
[build.environment]
  HUSKY = "0"
