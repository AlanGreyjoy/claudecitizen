name: Quality

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: quality-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  repository-safety:
    name: Repository safety
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Reject tracked protected assets
        shell: bash
        run: |
          unexpected="$({
            git ls-files \
              'assets/**' \
              'public/assets/protected/**' \
              'src/assets/protected/**'
          } | grep -Ev \
            '^(public/assets/protected/README\.md|src/assets/protected/(README\.md|characters/synty_sidekick/README\.md))$' \
            || true)"

          if [[ -n "$unexpected" ]]; then
            echo "Protected assets must never be committed:"
            echo "$unexpected"
            exit 1
          fi

  code-quality:
    name: Typecheck, lint, and build
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4 # stable
        with:
          toolchain: 1.96.0
          components: rustfmt, clippy
          targets: wasm32-unknown-unknown

      - name: Cache Cargo
        uses: Swatinem/rust-cache@42dc69e1aa15d09112580998cf2ef0119e2e91ae # v2
        with:
          workspaces: backend

      - name: Install dependencies
        run: npm ci

      - name: Typecheck browser
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Build editor renderer
        run: npm run build:editor:web

      - name: Build web release target
        run: npm run build:web

      - name: Build API server
        run: npm run build:server

      - name: Check Rust formatting
        working-directory: backend
        run: cargo fmt --all -- --check

      - name: Lint Rust backend
        working-directory: backend
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Build documentation
        run: npm run docs:build
