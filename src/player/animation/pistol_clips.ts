/** Handgun idle clip used by the stance switch (preload + resolve). */
export const PISTOL_IDLE_CLIP = 'pistol_idle' as const;

export const PISTOL_IDLE_CLIPS: readonly string[] = [PISTOL_IDLE_CLIP] as const;
